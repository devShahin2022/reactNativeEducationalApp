import { StyleSheet } from "react-native";

export default CommonStyles = StyleSheet.create({
    flex : {
        flexDirection:'row',
        flexWrap:'wrap',
        alignItems : 'center',
        width : "100%"
    }
})